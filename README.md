# Vector-Search-Engine
This is a small semantic search engine. It takes a set of text documents, converts each one into a numeric vector using a sentence embedding model, and lets you search them with a plain text query.
